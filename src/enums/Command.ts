/* eslint-disable no-unused-vars */
export enum Command {
  START = 'start',
  HOME = 'home',
  EXERCISES = 'exercises',
  TRAINING = 'training',
  MEASUREMENTS = 'measurements',
  PROFILE = 'profile',
  ADMIN = 'admin',
}
